/**
 * Persistence boundary for operation metadata and events.
 *
 * <p>JPA entities and repositories will be implemented by later dedicated tasks.
 * Media bytes must stay out of the database.</p>
 */
package com.lucasdourado.mediautility.persistence;
